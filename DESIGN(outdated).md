what is the state of hangman?
what variables are required?

def update_game_state(secret_word, guessed_letters, guess):

this function:
takes current state
returns updated state

no  ui
no loop
no input handling

constraint: must be pure (no global variables)

random word selection from list
display masked word
track incorrect guesses
win/lose detection

no while true loops
no global variables
no string replacement functions
must support phrases with spaces
must support case-insensitive guessing
must support replay without restarting program
must separate logic and UI layers completely

